# pre-run
tau new project -y \
    --name someProject1 \
    --description somedesc \
    --private \
    --no-embed-token \
    --color never

tau new project -y \
    --name someProject2 \
    --description somedesc \
    --private \
    --no-embed-token \
    --color never

tau new project -y \
    --name someProject3 \
    --description somedesc \
    --private \
    --no-embed-token \
    --color never

tau new project -y \
    --name someProject4 \
    --description somedesc \
    --private \
    --no-embed-token \
    --color never

tau new project -y \
    --name someProject5 \
    --description somedesc \
    --private \
    --no-embed-token \
    --color never

# command
tau query project \
    --list

